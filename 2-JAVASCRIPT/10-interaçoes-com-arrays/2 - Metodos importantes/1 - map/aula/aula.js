// map(): Aplica uma transformação a cada elemento do array original e retorna um novo array com os resultados.

const numeros = [10, 20, 30, 40, 50];

const quadrados = numeros.map(num => num * num);
console.log(quadrados);